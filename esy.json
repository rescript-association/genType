{
  "name": "gentype",
  "version": "2.41.0",
  "dependencies": {
    "ocaml": "~4.06.1",
    "@opam/dune": "~1.9.0",
    "@opam/reason": "*",
    "refmterr": "*"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "@opam/bisect_ppx": "^2.0.0"
  },
  "scripts": {
    "clean": "dune clean -p #{self.name}"
  },
  "esy": {
    "build": "dune build @check @all -p #{self.name}",
    "release": {
      "bin": [
        "gentype.exe"
      ]
    }
  }
}
