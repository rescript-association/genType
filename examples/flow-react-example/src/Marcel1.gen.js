/** 
 * @flow strict
 * @generated from Marcel1.res
 * @nolint
 */
/* eslint-disable */

// $FlowExpectedError[untyped-import]: Reason checked type sufficiently
import * as Marcel1BS from './Marcel1.bs';

export type M_t = {| +name: string, +surname: string |};

export type m = M_t;

export type N_t = {| +name: string, +surname: string |};

export type n = N_t;

export type O_t = {| +name: string, +surname: string |};

export type o = O_t;

export const testConversion: (n) => n = Marcel1BS.testConversion;
