import * as React from "react";

type Props = {
  readonly method?: "push" | "replace";
};

export const make: React.FC<Props> = (x: Props) => {
  return (
    <div>
    </div>
  );
};

export default make;
